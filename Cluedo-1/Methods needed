Need in Player class
List<Card> player.getCards() 			//needed to list cards for CMD
boolean player.hasCard(Card c)			//needed for testing refutes
getRoom()								//needed when adding the persons room for a suggestion



DONE:

board.moveSecretPassage(Player p)		//moves player through secret passage
										//throws an exception if isn't in a room with one
board.inRoom(Player p)					//I've left this in Player - this is what the Square interface is for.
board.inPoolRoom(Player p)				//returns true/ false if in pool-room
board.getAllBestPaths(Player p)			//this is done!  board.getDistanceToAllRooms(Player p);
